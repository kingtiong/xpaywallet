diff --git a/node_modules/@react-native-community/cli-platform-android/build/config/getAndroidProject.js b/node_modules/@react-native-community/cli-platform-android/build/config/getAndroidProject.js
index 305602e..1220d13 100644
--- a/node_modules/@react-native-community/cli-platform-android/build/config/getAndroidProject.js
+++ b/node_modules/@react-native-community/cli-platform-android/build/config/getAndroidProject.js
@@ -27,6 +27,16 @@ function _fs() {
   return data;
 }
 
+function _path() {
+  const data = _interopRequireDefault(require("path"));
+
+  _path = function () {
+    return data;
+  };
+
+  return data;
+}
+
 function _chalk() {
   const data = _interopRequireDefault(require("chalk"));
 
@@ -64,7 +74,21 @@ function getPackageName(manifestPath) {
   let packageNameMatchArray = androidManifest.match(/package="(.+?)"/);
 
   if (!packageNameMatchArray || packageNameMatchArray.length === 0) {
-    throw new (_cliTools().CLIError)(`Failed to build the app: No package name found. Found errors in ${_chalk().default.underline.dim(`${manifestPath}`)}`);
+    // Many modern RN libraries omit `package="..."` in their AndroidManifest.xml and rely on
+    // Gradle `namespace` instead. Fall back to parsing the library `android/build.gradle` when possible.
+    const namespace = findNamespaceFromGradle(manifestPath);
+
+    if (!namespace) {
+      throw new (_cliTools().CLIError)(`Failed to build the app: No package name found. Found errors in ${_chalk().default.underline.dim(`${manifestPath}`)}`);
+    }
+
+    let packageName = namespace;
+
+    if (!validatePackageName(packageName)) {
+      _cliTools().logger.warn(`Invalid application's package name "${_chalk().default.bgRed(packageName)}" resolved from Gradle namespace. Read guidelines for setting the package name here: ${_chalk().default.underline.dim('https://developer.android.com/studio/build/application-id')}`);
+    }
+
+    return packageName;
   }
 
   let packageName = packageNameMatchArray[1];
@@ -81,4 +105,43 @@ function validatePackageName(packageName) {
   return /^[a-z][a-z0-9_]*(\.[a-z][a-z0-9_]*)+$/i.test(packageName);
 }
 
+function findNamespaceFromGradle(manifestPath) {
+  try {
+    // Walk up to find the closest `android` directory.
+    let dir = _path().default.dirname(manifestPath);
+    let androidDir = null;
+
+    while (dir && dir !== _path().default.dirname(dir)) {
+      if (_path().default.basename(dir) === 'android') {
+        androidDir = dir;
+        break;
+      }
+      dir = _path().default.dirname(dir);
+    }
+
+    if (!androidDir) {
+      return null;
+    }
+
+    const gradlePaths = [_path().default.join(androidDir, 'build.gradle'), _path().default.join(androidDir, 'build.gradle.kts')];
+
+    for (const gradlePath of gradlePaths) {
+      if (!_fs().default.existsSync(gradlePath)) continue;
+      const content = _fs().default.readFileSync(gradlePath, 'utf8');
+
+      // Support both Groovy and Kotlin DSL variants:
+      // - namespace "com.example"
+      // - namespace = "com.example"
+      const match = content.match(/namespace\s*(?:=)?\s*["']([^"']+)["']/);
+      if (match && match[1]) {
+        return match[1];
+      }
+    }
+
+    return null;
+  } catch (e) {
+    return null;
+  }
+}
+
 //# sourceMappingURL=getAndroidProject.js.map
\ No newline at end of file
diff --git a/node_modules/@react-native-community/cli-platform-android/native_modules.gradle b/node_modules/@react-native-community/cli-platform-android/native_modules.gradle
index 4a63eaf..43fc838 100644
--- a/node_modules/@react-native-community/cli-platform-android/native_modules.gradle
+++ b/node_modules/@react-native-community/cli-platform-android/native_modules.gradle
@@ -393,26 +393,44 @@ class ReactNativeModules {
    */
   String getCommandOutput(String[] command, File directory) {
     try {
-      def output = ""
       def cmdProcess = Runtime.getRuntime().exec(command, null, directory)
-      def bufferedReader = new BufferedReader(new InputStreamReader(cmdProcess.getInputStream()))
-      def buff = ""
-      def readBuffer = new StringBuffer()
-      while ((buff = bufferedReader.readLine()) != null) {
-        readBuffer.append(buff)
+      def stdoutBuffer = new StringBuffer()
+      def stderrBuffer = new StringBuffer()
+
+      def stdoutReader = new BufferedReader(new InputStreamReader(cmdProcess.getInputStream(), "UTF-8"))
+      def stderrReader = new BufferedReader(new InputStreamReader(cmdProcess.getErrorStream(), "UTF-8"))
+
+      def stdoutThread = Thread.start {
+        def line = ""
+        while ((line = stdoutReader.readLine()) != null) {
+          stdoutBuffer.append(line).append("\n")
+        }
       }
-      output = readBuffer.toString()
-      if (!output) {
-        this.logger.error("${LOG_PREFIX}Unexpected empty result of running '${command}' command.")
-        def bufferedErrorReader = new BufferedReader(new InputStreamReader(cmdProcess.getErrorStream()))
-        def errBuff = ""
-        def readErrorBuffer = new StringBuffer()
-        while ((errBuff = bufferedErrorReader.readLine()) != null) {
-          readErrorBuffer.append(errBuff)
+      def stderrThread = Thread.start {
+        def line = ""
+        while ((line = stderrReader.readLine()) != null) {
+          stderrBuffer.append(line).append("\n")
         }
-        throw new Exception(readErrorBuffer.toString())
       }
-      return output
+
+      def exitCode = cmdProcess.waitFor()
+      stdoutThread.join()
+      stderrThread.join()
+
+      def stdout = stdoutBuffer.toString()
+      def stderr = stderrBuffer.toString()
+
+      if (exitCode != 0) {
+        this.logger.error("${LOG_PREFIX}Running '${command}' command failed with exit code ${exitCode}.")
+        throw new Exception("Command failed: ${command}\nexitCode=${exitCode}\nstdout=${stdout}\nstderr=${stderr}")
+      }
+
+      if (!stdout) {
+        this.logger.error("${LOG_PREFIX}Unexpected empty result of running '${command}' command.")
+        throw new Exception(stderr ?: "Empty output from command: ${command}")
+      }
+
+      return stdout.trim()
     } catch (Exception exception) {
       this.logger.error("${LOG_PREFIX}Running '${command}' command failed.")
       throw exception
@@ -438,14 +456,31 @@ class ReactNativeModules {
      */
     def cliResolveScript = "try {console.log(require('@react-native-community/cli').bin);} catch (e) {console.log(require('react-native/cli').bin);}"
     String[] nodeCommand = ["node", "-e", cliResolveScript]
-    def cliPath = this.getCommandOutput(nodeCommand, this.root)
+    def cliPathRaw = this.getCommandOutput(nodeCommand, this.root)
+    // Be defensive: some environments may emit additional lines or line endings here.
+    // We select the last non-empty line as the CLI entry point.
+    def cliPathLines = cliPathRaw
+      ?.readLines()
+      ?.collect { it?.trim() }
+      ?.findAll { it }
+    def cliPath = (cliPathLines && cliPathLines.size() > 0) ? cliPathLines.last() : cliPathRaw?.trim()
 
     String[] reactNativeConfigCommand = ["node", cliPath, "config"]
     def reactNativeConfigOutput = this.getCommandOutput(reactNativeConfigCommand, this.root)
 
     def json
     try {
-      json = new JsonSlurper().parseText(reactNativeConfigOutput)
+      def sanitizedConfigOutput = reactNativeConfigOutput?.trim()
+      // Some environments/log levels may emit non-JSON lines before/after the JSON payload.
+      // Gradle expects JSON here, so we attempt to extract the JSON object from the output.
+      if (sanitizedConfigOutput && !sanitizedConfigOutput.startsWith("{")) {
+        def startIdx = sanitizedConfigOutput.indexOf("{")
+        def endIdx = sanitizedConfigOutput.lastIndexOf("}")
+        if (startIdx >= 0 && endIdx > startIdx) {
+          sanitizedConfigOutput = sanitizedConfigOutput.substring(startIdx, endIdx + 1)
+        }
+      }
+      json = new JsonSlurper().parseText(sanitizedConfigOutput)
     } catch (Exception exception) {
       throw new Exception("Calling `${reactNativeConfigCommand}` finished with an exception. Error message: ${exception.toString()}. Output: ${reactNativeConfigOutput}");
     }
